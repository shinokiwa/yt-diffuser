/**
 * Thumbnail.vue のテスト
 */
import { expect, describe, it } from 'vitest'
import { mount } from '@vue/test-utils'
import ImageThumb from '../ImageThumb.vue'

describe('サムネイル表示コンポーネント', () => {
  it.todo('あとで考える。', () => {
    expect(true).toBe(true)
  })
})
