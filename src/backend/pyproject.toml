[tool.pytest.ini_options]
log_cli_level = "DEBUG"
markers = [
    "describe: Description of the test context",
    "it: Description of the test case",
    "test: Description of the test context"
]
env = [
    "TEST=1"
]

[tool.coverage.run]
branch = true